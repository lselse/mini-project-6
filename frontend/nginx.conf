server {
    listen 80;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        
        # 새로고침 시 404 안 뜨게 index.html로 돌려주는 마법의 코드
        try_files $uri $uri/ /index.html;
    }
}